@model IEnumerable<WebApplication2.Models.Color>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<div class="w3-container">


    <table class="w3-table-all w3-hoverable">
        <thead>
            <tr class="w3-light-grey">
                <th>@Html.DisplayNameFor(model => model.ColorName)</th>
                <th>@Html.DisplayNameFor(model => model.ColorOrder)</th>
                <th>Actions</th>
            </tr>
        </thead>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.ColorName)</td>
                <td>@Html.DisplayFor(modelItem => item.ColorOrder)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ColorId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ColorId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ColorId">Delete</a>
                </td>
            </tr>
        }
    </table>
</div>